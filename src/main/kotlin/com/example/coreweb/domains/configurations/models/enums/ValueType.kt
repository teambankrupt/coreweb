package com.example.coreweb.domains.configurations.models.enums

enum class ValueType {
    STRING,
    BOOLEAN,
    BYTE,
    SHORT,
    INTEGER,
    INT,
    LONG,
    FLOAT,
    DOUBLE,
    CHAR;
}